name: Jericho Beach Wind Alert

on:
  schedule:
    # Off-the-hour minutes — GitHub Actions cron queues are heaviest at :00.
    - cron: '17 14 * * *'   # 14:17 UTC ≈ 7:17am PT
    - cron: '37 23 * * *'   # 23:37 UTC ≈ 4:37pm PT
  workflow_dispatch:

concurrency:
  group: wind-alert
  cancel-in-progress: false

jobs:
  check-wind:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    env:
      PYTHONUNBUFFERED: '1'
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'
      - name: Cache venv
        uses: actions/cache@v5
        with:
          path: .venv
          key: venv-${{ runner.os }}-py3.12-${{ hashFiles('requirements.txt') }}
      - name: Install (cache miss only)
        run: |
          if [ ! -d .venv ]; then
            python -m venv .venv
            .venv/bin/pip install -r requirements.txt
          fi
      - name: Run wind check
        env:
          NTFY_TOPIC: ${{ secrets.NTFY_TOPIC }}
        run: .venv/bin/python -u check_wind.py
